# Copy this file to .env and fill in your real key. .env is gitignored —
# never commit real secrets. Get a key from https://console.anthropic.com/

# Used server-side only, by functions in /api — never exposed to the browser.
ANTHROPIC_API_KEY=sk-ant-your-key-here
